{
  "_comment": "Optional path-scoping for a project-local skill. Drop next to SKILL.md to limit auto-invocation to matching paths. See core-rules/inheritance.md > 'Skill path-scoping' for the schema and why this is project-local-only.",
  "paths": [
    "services/**",
    "pkg/**"
  ],
  "reason": "Go-only validators; would add noise on the Next.js subtree."
}
